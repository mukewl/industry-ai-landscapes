# FINDINGS.md — Insight register

> One entry per insight/pattern, with an **evidence pointer** (company names + which columns support it). Findings without evidence pointers are not findings.
> Statuses: `hypothesis` → `verified` / `fixed` (led to a workbook correction) / `retracted` (keep retracted entries — they document what NOT to rely on).

| ID | Status | Title | Evidence |
|---|---|---|---|
| F1 | hypothesis | <title> | <companies + columns> |
