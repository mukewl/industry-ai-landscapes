# DECISIONS.md — Append-only decision log

> One dated line (or short block) per decision. Never rewrite history; if a decision is reversed, append the reversal.
> Log the adaptation choices first: incumbent + benchmark profile, index name, signal groups, pillars, value-chain stages, sector taxonomy + colors, protocol flags, quadrant labels (DATA-CONTRACT.md §5).

- **<YYYY-MM-DD> · D1** — <decision> (<who chose>.)
