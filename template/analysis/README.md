# analysis/ — the written deliverables

Numbered by workstream (the reference project used: `01-threat-ranking.md`, `02-relationship-web.md`, `03-synergy-mapping.md`, `04-hidden-patterns.md`). Each file starts with a **Status** line (`draft` / `reviewed by user` / `final`) and a **Last updated** date. Every claim about a company cites its row, e.g. `(<INDEX> 78, High threat, Imminent threat quadrant)`.

These documents are the archival source of record; the dashboard is the interactive view on top of them.
