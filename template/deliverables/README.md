# deliverables/ — client-facing outputs

Decks, exported reports, and their build scripts (the reference project kept a PPTX + a `build_deck.js` here). Content is drawn from `analysis/` and FINDINGS.md — deliverables restate verified analysis, they don't originate it.
